{
  "name": "qr-light-transfer-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Node.js HTTPS host for the QR Light Transfer frontend",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check main.js && node --check server.mjs",
    "start": "node server.mjs"
  }
}
